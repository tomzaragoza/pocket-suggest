// Prediket = Ember.Application.createWithMixins(Bootstrap.Register);
Prediket =  Ember.Application.createWithMixins(Bootstrap.Register);

Prediket.Stream = Ember.Object.extend({


// loadStreams: function() {
// var streams = this;
// return Em.Deferred.promise(function(p) {
// p.resolve($.getJSON("/streams")).then(function(respose){
// var allStreams = Em.A();
// response.forEach(function(child) {
// allStreams.pushObject(child);
// });
// streams.setProperties({allStreams: allStreams});
// return allStreams;
// });
// });
// }
});
